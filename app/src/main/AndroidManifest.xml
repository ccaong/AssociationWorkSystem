<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gqsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.ProjectDoingActivity" />
        <activity android:name=".ui.activity.ProjectToDoActivity" />
        <activity android:name=".ui.standardization.StandardizationActivity" />
        <activity android:name=".ui.doubleprevention.DoublePreventionActivity" />
        <activity android:name=".ui.evaluate.EvaluateActivity" />
        <activity android:name=".ui.securitymanagement.SecurityManagementActivity" />
        <activity android:name=".ui.informationize.InformationizeActivity" />
        <activity android:name=".ui.govproject.GovProjectActivity" />
        <activity android:name=".ui.specialrectification.SpecialRectificationActivity" />
    </application>

</manifest>